-- Elm runs in the browser and has no file system access.
-- This is a placeholder showing the language structure.
module Main exposing (main)
import Html exposing (text)

main =
    text "Elm has no file I/O — see ../data/phrase.txt manually"
